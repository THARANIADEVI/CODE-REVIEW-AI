# Base URL of the deployed backend API, including the /api prefix.
# Leave unset for local dev (Vite proxies /api to http://localhost:5000).
# Example for Render: https://ai-code-review-assistant-api.onrender.com/api
VITE_API_BASE_URL=
