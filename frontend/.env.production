# Baked into the production build (`vite build`) so the deployed frontend calls the
# real backend instead of a relative /api on its own host (which 404s -> "Registration
# failed"). Override in the Vercel dashboard if the backend URL changes.
VITE_API_BASE_URL=https://code-review-ai-aub7.onrender.com/api
